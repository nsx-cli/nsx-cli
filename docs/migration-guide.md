# Migration Guide

## From pre-1.0 to 1.0.0

- Use command resolution through Bootstrap/ApplicationContext
- Prefer `make` for high-level scaffolding
- Use `prisma crud` for DMMF-backed generation
- Use `fix` and `graph` for architecture maintenance

## Breaking Changes

No intentional breaking changes were introduced in 1.0.0 release prep.
